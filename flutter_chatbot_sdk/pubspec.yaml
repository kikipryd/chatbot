name: flutter_chatbot_sdk
description: A customizable, production-ready AI Chatbot SDK with RAG capabilities connected to SaaS Chatbot backend. Works on Android, iOS, Web, Windows, macOS, and Linux.
version: 1.0.0
homepage: https://example.com/saas_chatbot

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
