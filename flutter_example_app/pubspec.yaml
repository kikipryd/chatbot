name: flutter_example_app
description: A beautiful demo application showing off the local SaaS AI Chatbot SDK with customized streaming answers.
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_chatbot_sdk:
    path: ../flutter_chatbot_sdk

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
